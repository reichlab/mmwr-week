// Convert MMWR weeks <--> dates


/**
 * Return javascript date object for Sunday of given week
 */
export const MMWRWeekToDate = (year, week) => {
  return null
}

/**
 * Convert given date (default now) to MMWRWeek
 */
export const DateToMMWRWeek = (date = new Date()) => {
  return null
}

/**
 * Return the number of MMWR weeks in a year
 */
export const MMWRWeeksInYear = year => {
  return null
}
